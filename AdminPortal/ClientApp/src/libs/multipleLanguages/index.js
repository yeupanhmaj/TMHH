
export const message = { 
    "menu_home_title" : "home",
    "menu_user_title" : "List Users "
}
// import * as GlobalStore  from '../../stores/global';
// import * as EN from './en'
// import * as VN from './VN' 
// import { GlobalObject } from '../../stores/global';

// // export const MlMessage = function (prop ){
// //     i
// // }

// const  MlMessage = ({ component: Conponent, ...rest }: IProps) => {
//     return (
//         <Route {...rest} render={props => (
//             isAuthenticated() ? (
//                 <Conponent {...props} />
//             ) : (
//                     <Redirect to={{
//                         pathname: '/login',
//                         state: { from: props.location },
//                     }} />
//                 )
//         )}
//         />
//     );
// };

// export default connect(
//     (state: GlobalObject.ApplicationObject) => state, // Selects which state properties are merged into the component's props
// )(MlMessage)


